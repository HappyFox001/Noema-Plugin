{
  "id": "skills-manager",
  "name": "Skills Manager",
  "description": "在任务运行时管理和检索本地 Skills。任务模型可以列出、搜索、读取或添加技能说明，再按技能内容执行任务。",
  "version": "0.1.0",
  "type": "sdk-plugin",
  "main": "index.mjs",
  "enabled": true,
  "permissions": [
    "tools.register",
    "filesystem.read",
    "network",
    "admin.custom"
  ],
  "config": {
    "maxSkillChars": 12000
  },
  "configSchema": [
    {
      "key": "maxSkillChars",
      "label": "读取上限",
      "description": "skills_read 返回的最大字符数。",
      "type": "number",
      "default": 12000,
      "min": 1000,
      "max": 50000,
      "step": 1000
    }
  ],
  "i18n": {
    "zh-CN": {
      "name": "Skills 管理器",
      "description": "在任务运行时管理和检索本地 Skills。任务模型可以列出、搜索、读取或添加技能说明，再按技能内容执行任务。",
      "config.maxSkillChars.label": "读取上限",
      "config.maxSkillChars.description": "skills_read 返回的最大字符数。"
    },
    "en-US": {
      "name": "Skills Manager",
      "description": "Manages and retrieves local Skills during task runtime. The task model can list, search, read, or add skill instructions before using them.",
      "config.maxSkillChars.label": "Read Limit",
      "config.maxSkillChars.description": "Maximum number of characters returned by skills_read."
    }
  }
}
